# This is a Useless App that creates a window with a button that when pressed changes the colors of the background.

## How to run:
* Compile the code using **gcc Useless_App.c -o SurpriseApp.exe -mwindows**
* Run the program using **.\SurpriseApp.exe**
